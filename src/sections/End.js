import React from 'react';
import styled from "styled-components";

const Section = styled.section`
    width: 100vw;
    height: 100vh;
    position: relative;
    z-index: 5;

    //background: white;
    display: flex;
    align-items: center;
`;

const End = () => {
    return (
        <Section>

        </Section>
    );
};

export default End;